import React from 'react'
import { useSelector } from 'react-redux'

function page() {
    // const { products, totalItemsCount } = useSelector((state) => state.product)

    return (
        <div>
            {/* {products && products.map((product) => (
                <div>
                    <h1>{product.title}</h1>
                </div>
            ))} */}
        </div>
    )
}

export default page